# Copy to .env and `set -a; . ./.env; set +a` before `bash serve/serve.sh`.
API_KEY=change-me-long-random-string
MODEL_PATH=/root/model-glm53-radix
SERVED_MODEL_NAME=glm-5.3-flash
# CONC=128           # max running requests = decode CUDA-graph ceiling (96 is the throughput sweet spot, 128 fits)
# CONTEXT_LENGTH=262144
# HF_TOKEN=hf_...    # only for gated/private repos; RadixArk/GLM-5.3-Flash-NVFP4 is public
